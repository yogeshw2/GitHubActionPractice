name: 01 - Building Block

on:  push 

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run : echo "Hello World!" 
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: failed step 
        run: |
          echo "i failed"    
          exit -1
      - name : say goodbye
        run : echo "goodbye!!"
